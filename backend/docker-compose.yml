version: '3.8'

services:
  app:
    build: .
    container_name: zyncgram-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./session.dat:/root/session.dat
